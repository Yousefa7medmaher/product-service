apiVersion: v1
kind: Service
metadata:
  name: product-svclb
  namespace: micro-app
spec:
  type: LoadBalancer
  selector:
    app: product-app
  ports:
    - protocol: TCP
      port: 3000        
      targetPort: 3000   
